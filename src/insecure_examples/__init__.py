"""Training material — deliberately insecure.

Every module in this package is a seeded example for the Lab 4.2 SAST
exercise: a plausible thing an assistant might actually generate, not an
obvious CTF puzzle. This package is never imported by the real
application -- see ``src/api/app.py``, which does not reference it -- so
none of this code runs. Do not copy anything from this package into real
work.
"""
